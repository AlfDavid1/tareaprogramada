<apex:page controller="userController">
    
    {! $User.Username} {!$Profile.Name}  {!$User.Id } 
    <apex:form >
        <apex:pageBlock >
            <apex:pageBlockSection >
                <apex:pageBlockSectionItem >
                    <apex:commandButton value="programar" action="{!actualizarOps}" />
                </apex:pageBlockSectionItem>
            </apex:pageBlockSection>
        </apex:pageBlock>
    </apex:form>
</apex:page>